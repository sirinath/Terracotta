<?xml version="1.0" encoding="UTF-8"?>
<!-- Apache Maven 2 POM generated by Apache Ivy http://ant.apache.org/ivy/ 
  Apache Ivy version: 2.2.0 20100923230623 -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.terracotta</groupId>
    <artifactId>terracotta-parent</artifactId>
    <version>3.6.0-SNAPSHOT</version>
    <relativePath>../terracotta-parent</relativePath>
  </parent>

  <groupId>org.terracotta.internal</groupId>
  <artifactId>terracotta-kit</artifactId>
  <packaging>jar</packaging>
  <name>terracotta-kit</name>
  
  <properties>
    <skip.deploy>true</skip.deploy>
    <rootDir>${project.build.directory}/terracotta-${project.version}</rootDir>  
    <toolkit.version>4.0.0-SNAPSHOT</toolkit.version>
    <ehcache.version>2.5.0-SNAPSHOT</ehcache.version>
    <quartz.version>2.1.0-SNAPSHOT</quartz.version>    
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.terracotta</groupId>
      <artifactId>terracotta</artifactId>
      <version>${project.version}</version>
    </dependency>
    
    <!-- for packaging purpose -->
    <dependency>
      <groupId>org.terracotta.internal</groupId>
      <artifactId>build-data</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.terracotta</groupId>
      <artifactId>terracotta-toolkit-1.4-runtime</artifactId>
      <version>${toolkit.version}</version>
      <scope>provided</scope>
    </dependency>       
    
    <!-- ehcache -->
    <dependency>
      <groupId>net.sf.ehcache</groupId>
      <artifactId>ehcache-core</artifactId>
      <version>${ehcache.version}</version>
      <classifier>distribution</classifier>
      <type>tar.gz</type>
      <scope>provided</scope>
    </dependency>     
    <dependency>
      <groupId>net.sf.ehcache</groupId>
      <artifactId>ehcache-terracotta</artifactId>
      <version>${ehcache.version}</version>
      <scope>provided</scope>
    </dependency>    
    <dependency>
      <groupId>org.terracotta.samples.ehcache</groupId>
      <artifactId>colorcache</artifactId>
      <version>${project.version}</version>
      <classifier>distribution</classifier>
      <type>tar.gz</type>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.terracotta.samples.ehcache.hibernate</groupId>
      <artifactId>events</artifactId>
      <version>${project.version}</version>
      <classifier>distribution</classifier>
      <type>tar.gz</type>
      <scope>provided</scope>
    </dependency>

    <!-- quartz 
    <dependency>
      <groupId>org.quartz-scheduler</groupId>
      <artifactId>quartz</artifactId>
      <version>${quartz.version}</version>
      <type>tar.gz</type>
      <scope>provided</scope>
    </dependency-->
      
    
  </dependencies>
      
  <build>
    <plugins>
      <plugin>
        <groupId>com.goldin.plugins</groupId>
        <artifactId>maven-copy-plugin</artifactId>
        <version>0.2.2</version>
        <configuration>
          <groovyConfig>
              <classpath>${project.basedir}/src/scripts</classpath>
          </groovyConfig>        
          <verbose>false</verbose>
          <resources>
            <!-- prepare skeleton structure of the kit -->
            <resource>
              <targetPath>${rootDir}</targetPath>
              <directory>${basedir}/src/packaging</directory>
              <preservePath>true</preservePath>
            </resource>
            
            <!-- populate lib folder -->
            <!--resource>
              <targetPath>${rootDir}/lib</targetPath>
              <dependency>
                <includeScope>compile</includeScope>
                <excludeArtifactIds>terracotta,build-data,thirdparty-api,dso-cluster-api,stax-api</excludeArtifactIds>
              </dependency>
              <dependenciesAtM2>true</dependenciesAtM2>
            </resource-->
            
            <!-- remove build-data.txt from terracotta jar -->
            <!--resource>
              <targetPath>${rootDir}/lib</targetPath>
              <dependency>
                <groupId>org.terracotta</groupId>
                <artifactId>terracotta</artifactId>
              </dependency>
              <process>{{ ('zip -d ' + files.first() + ' build-data.txt').execute().text }}</process>
            </resource-->
            
            <!-- build data -->
            <!--resource>
              <targetPath>${rootDir}/lib/resources</targetPath>
              <dependency>
                <groupId>org.terracotta.internal</groupId>
                <artifactId>build-data</artifactId>
              </dependency>
              <zipEntries>
                <zipEntry>build-data.txt</zipEntry>
                <zipEntry>tim-get.properties</zipEntry>
              </zipEntries>
              <unpack>true</unpack>              
            </resource-->
            
            <!-- tookit -->
            <!--resource>
              <targetPath>${rootDir}/common</targetPath>
              <dependency>
                <groupId>org.terracotta</groupId>
                <artifactId>terracotta-toolkit-1.4-runtime</artifactId>
              </dependency>            
            </resource>
            <resource>
              <targetPath>${rootDir}/common/docs/javadoc</targetPath>
              <dependency>
                <groupId>org.terracotta</groupId>
                <artifactId>terracotta-toolkit-1.4-runtime</artifactId>
                <classifier>javadoc</classifier>
              </dependency>
              <unpack>true</unpack>
            </resource-->            
            
            <!-- ehcache-core -->
            <resource>
              <targetPath>${rootDir}</targetPath>
              <dependencies>
                <dependency>
                  <groupId>net.sf.ehcache</groupId>
                  <artifactId>ehcache-core</artifactId>
                  <classifier>distribution</classifier>
                  <type>tar.gz</type>
                </dependency>
              </dependencies>
              <unpack>true</unpack>
              <process>{{
              Util.rename(project.properties['rootDir']  + "/ehcache-core-${ehcache.version}", project.properties['rootDir']  + "/ehcache") 
              }}
              </process>              
            </resource> 
            
            <!-- ehcache terracotta -->
            <resource>
              <targetPath>${rootDir}/ehcache/lib</targetPath>
              <dependencies>
                <dependency>
                  <groupId>net.sf.ehcache</groupId>
                  <artifactId>ehcache-terracotta</artifactId>
                </dependency>
              </dependencies>
            </resource>
            
            <!-- ehcache samples-->
            <resource>
              <targetPath>${rootDir}/ehcache/samples</targetPath>
              <dependencies>
                <dependency>
                  <groupId>org.terracotta.samples.ehcache</groupId>
                  <artifactId>colorcache</artifactId>
                  <classifier>distribution</classifier>
                  <type>tar.gz</type>
                </dependency>
                <dependency>
                  <groupId>org.terracotta.samples.ehcache.hibernate</groupId>
                  <artifactId>events</artifactId>
                  <classifier>distribution</classifier>
                  <type>tar.gz</type>
                </dependency>
              </dependencies>
              <unpack>true</unpack>
            </resource>             
            
            <!-- package tar.gz kit -->
            <!--resource>
              <targetPath>${rootDir}.tar.gz</targetPath>
              <directory>${rootDir}</directory>
              <pack>true</pack>
            </resource-->
            
          </resources>
        </configuration>
      </plugin>
    </plugins>
  </build>
      

</project>
