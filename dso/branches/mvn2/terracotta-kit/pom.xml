<?xml version="1.0" encoding="UTF-8"?>
<!-- Apache Maven 2 POM generated by Apache Ivy http://ant.apache.org/ivy/ 
  Apache Ivy version: 2.2.0 20100923230623 -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.terracotta</groupId>
    <artifactId>terracotta-parent</artifactId>
    <version>3.6.0-SNAPSHOT</version>
    <relativePath>../terracotta-parent</relativePath>
  </parent>

  <groupId>org.terracotta.internal</groupId>
  <artifactId>terracotta-kit</artifactId>
  <packaging>jar</packaging>
  <name>terracotta-kit</name>

  <dependencies>
    <dependency>
      <groupId>org.terracotta</groupId>
      <artifactId>terracotta</artifactId>
      <version>${project.version}</version>
    </dependency>
    
    <!-- for packaging purpose -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
    </dependency>
    
  </dependencies>
  
  <properties>
    <skip.deploy>true</skip.deploy>
    <rootDir>${project.build.directory}/${project.artifactId}-${project.version}</rootDir>
  </properties>
      
  <build>
    <plugins>
      <plugin>
        <groupId>com.goldin.plugins</groupId>
        <artifactId>maven-copy-plugin</artifactId>
        <version>0.2.2</version>
        <configuration>
          <verbose>false</verbose>
          <resources>
            <!-- prepare skeleton structure of the kit -->
            <resource>
              <targetPath>${rootDir}</targetPath>
              <directory>${basedir}/src/packaging</directory>
              <preservePath>true</preservePath>
            </resource>
            
            <!-- populate lib folder -->
            <resource>
              <targetPath>${rootDir}/lib</targetPath>
              <dependency>
                <includeScope>compile</includeScope>
                <excludeArtifactIds>thirdparty-api,dso-cluster-api,stax-api</excludeArtifactIds>
              </dependency>
            </resource>
            
            <!-- package tar.gz kit -->
            <resource>
              <targetPath>${rootDir}.tar.gz</targetPath>
              <directory>${rootDir}</directory>
              <pack>true</pack>
            </resource>
          </resources>
        </configuration>
      </plugin>
    </plugins>
  </build>
      

</project>
